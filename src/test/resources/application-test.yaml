spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hr_test?serverTimezone=UTC
    username: root
    password: password
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    properties:
      hibernate:
      format_sql: true


  jackson:
    serialization:
      fail-on-empty-beans: false

  hibernate:
      ddl-auto: none

